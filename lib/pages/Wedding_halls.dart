
import 'package:flutter/widgets.dart';

class Wedding_halls extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      child: Center(child: Text("welcome to wedding halls")),
    );
    throw UnimplementedError();
  }

}