
import 'package:flutter/cupertino.dart';

class profile extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      child: Center(child: Text("welcome to profile page")),
    );
    throw UnimplementedError();
  }

}